<%# Header %>
<div class="flex justify-between items-center mb-6">
  <div>
    <h1 class="text-2xl font-semibold text-gray-900">Edit Price</h1>
    <p class="mt-1 text-sm text-gray-600">
      <%= @price.channel.name %> • <%= l @price.applicable_at, format: :long %>
    </p>
  </div>
</div>

<%# Form %>
<%= render 'form' %>
